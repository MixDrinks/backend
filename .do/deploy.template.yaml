spec:
  name: mixdrinks-api
  services:
    - name: web
      git:
        branch: main
        repo_clone_url: https://github.com/MixDrinks/backend.git
      envs:
        - key: DB_URL
          scope: RUN_TIME
        - key: DB_USER
          scope: RUN_TIME
        - key: DB_PASSWORD
          scope: RUN_TIME
